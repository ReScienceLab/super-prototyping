Screenshots referenced from pull request bodies.

This is an orphan branch. It holds no source, is never merged, and exists
only so a PR description can embed an image over raw.githubusercontent.com.
One folder per PR number.
