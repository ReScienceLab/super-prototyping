# Cloudflare Pages: a community project's boards, at /p/<id>/ (vite.config.ts), are other
# people's HTML, so each runs in an origin of its own, as server/sp.ts serves a board (SANDBOX).
/p/:id/board/*
  Content-Security-Policy: sandbox allow-scripts allow-forms allow-popups allow-modals allow-downloads
